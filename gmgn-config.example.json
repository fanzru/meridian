{
  "_note": "Optional. GMGN OpenAPI is used as the fee source for the minTokenFeesSol gate (more accurate than Jupiter's `fees`). Copy to gmgn-config.json for non-secret settings only; keep apiKey blank here and set GMGN_API_KEY in .env. Set feeSource to \"jupiter\" to disable.",
  "apiKey": "",
  "baseUrl": "https://openapi.gmgn.ai",
  "requestDelayMs": 2500,
  "maxRetries": 2,
  "feeSource": "gmgn"
}
